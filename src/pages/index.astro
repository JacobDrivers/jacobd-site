---
const tools = [
  {
    name: "Coin & Currency Scout",
    desc: "Spot prices, melt calculator, key dates, and inventory. Built for quick decisions at home or at auctions.",
    href: "/tools/coin-scout",
    status: "Live",
    tag: "Tools",
  },
  {
    name: "Coin Library (coming soon)",
    desc: "Quick reference for types, compositions, key dates, and red flags.",
    href: "/tools",
    status: "Soon",
    tag: "Reference",
    disabled: true,
  },
  {
    name: "Auction Mode (coming soon)",
    desc: "Fast entry, totals, targets, and sanity checks while bidding.",
    href: "/tools",
    status: "Soon",
    tag: "Workflow",
    disabled: true,
  },
];

const links = [
  { name: "GitHub", href: "https://github.com/JacobDrivers" },
  { name: "LinkedIn", href: "https://www.linkedin.com/in/jacobadrury/" },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Jacob Drury | Tools + Projects</title>
    <meta
      name="description"
      content="Personal site for small, useful tools and projects. Fast, shareable, and practical."
    />
    <style>
      :root {
        --bg: #0b1220;
        --panel: rgba(255, 255, 255, 0.06);
        --panel2: rgba(255, 255, 255, 0.09);
        --text: rgba(255, 255, 255, 0.92);
        --muted: rgba(255, 255, 255, 0.72);
        --faint: rgba(255, 255, 255, 0.55);
        --stroke: rgba(255, 255, 255, 0.10);
        --accent: #f59e0b;
        --good: #34d399;
        --shadow: 0 20px 60px rgba(0, 0, 0, 0.45);
        --radius: 16px;
      }

      * { box-sizing: border-box; }
      html, body { height: 100%; }
      body {
        margin: 0;
        min-height: 100vh;
        background: var(--bg);
        color: var(--text);
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
        position: relative;
        overflow-x: hidden;
      }

      body::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 200vh;
        z-index: -1;
        background:
          radial-gradient(1800px 1200px at 12% -10%, rgba(245, 158, 11, 0.12) 0%, transparent 65%),
          radial-gradient(1600px 1200px at 110% 20%, rgba(52, 211, 153, 0.06) 0%, transparent 70%),
          radial-gradient(2200px 1500px at 50% 120%, rgba(59, 130, 246, 0.05) 0%, transparent 85%),
          var(--bg);
        background-repeat: no-repeat;
        background-size: cover;
        pointer-events: none;
      }

      @media (max-width: 860px) {
        /* Increase overlay height slightly on mobile for better full-screen coverage */
        body::before {
          height: 220vh;
        }
      }

      a { color: inherit; text-decoration: none; }
      a:focus-visible { outline: 2px solid rgba(245, 158, 11, 0.7); outline-offset: 3px; border-radius: 10px; }

      .wrap {
        max-width: 1050px;
        margin: 0 auto;
        padding: 28px 20px 60px;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      header {
        display: flex;
        align-items: flex-start;
        justify-content: flex-start;
        gap: 18px;
        margin-top: 14px;
        margin-bottom: 28px;
      }

      .brand h1 {
        font-size: clamp(28px, 3.4vw, 40px);
        letter-spacing: -0.02em;
        margin: 0 0 8px;
        line-height: 1.05;
      }

      .brand p {
        margin: 0;
        color: var(--muted);
        max-width: 70ch;
        line-height: 1.5;
      }

      @media (max-width: 860px) {
        header { flex-direction: column; align-items: flex-start; }
      }

      .hero {
        margin-top: 0;
        border: 1px solid var(--stroke);
        background: linear-gradient(180deg, var(--panel2), rgba(255,255,255,0.03));
        border-radius: var(--radius);
        padding: 24px;
        box-shadow: var(--shadow);
      }

      .cta {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        margin-top: 12px;
      }

      .btn {
        border: 1px solid var(--stroke);
        background: rgba(255,255,255,0.06);
        padding: 10px 14px;
        border-radius: 12px;
        font-weight: 600;
        font-size: 14px;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        transition: transform 120ms ease, background 120ms ease, border-color 120ms ease;
      }
      .btn:hover { transform: translateY(-1px); background: rgba(255,255,255,0.09); border-color: rgba(255,255,255,0.18); }
      .btnPrimary {
        background: rgba(245, 158, 11, 0.14);
        border-color: rgba(245, 158, 11, 0.35);
      }
      .btnPrimary:hover {
        background: rgba(245, 158, 11, 0.20);
        border-color: rgba(245, 158, 11, 0.48);
      }

      .cards {
        margin-top: 16px;
        display: grid;
        grid-template-columns: 1fr;
        gap: 12px;
      }

      .card {
        border: 1px solid var(--stroke);
        background: rgba(255,255,255,0.05);
        border-radius: var(--radius);
        padding: 14px;
        min-height: 140px;
        transition: transform 120ms ease, background 120ms ease, border-color 120ms ease;
      }
      .card:hover { transform: translateY(-1px); background: rgba(255,255,255,0.07); border-color: rgba(255,255,255,0.18); }
      .card.disabled { opacity: 0.60; }
      .card.disabled:hover { transform: none; }

      .cardTop {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
      }

      .badge {
        font-size: 12px;
        color: rgba(255,255,255,0.78);
        border: 1px solid var(--stroke);
        background: rgba(255,255,255,0.05);
        padding: 6px 10px;
        border-radius: 999px;
        white-space: nowrap;
      }

      .badgeLive { border-color: rgba(52, 211, 153, 0.30); background: rgba(52, 211, 153, 0.10); }
      .badgeSoon { border-color: rgba(255, 255, 255, 0.16); }

      .card h3 { margin: 10px 0 6px; font-size: 16px; letter-spacing: -0.01em; }
      .card p { margin: 0; color: var(--muted); line-height: 1.45; font-size: 14px; }
    </style>
  </head>

  <body>
    <div class="wrap">
      <header>
        <div class="brand">
          <h1>Tools & quick answers</h1>
          <p>
            No accounts. No tracking. Just fast, shareable calculators and references built for real use.
          </p>
        </div>
      </header>

      <main class="hero">
        <section>
          <div style="margin-bottom: 18px; display: flex; gap: 8px; flex-wrap: wrap; align-items: center;">
            <span style="color: var(--good); font-size: 13px; font-weight: 600;">✓ No accounts</span>
            <span style="color: var(--faint);">•</span>
            <span style="color: var(--good); font-size: 13px; font-weight: 600;">✓ No tracking</span>
            <span style="color: var(--faint);">•</span>
            <span style="color: var(--good); font-size: 13px; font-weight: 600;">✓ Fast & static</span>
          </div>

          <h2 style="margin: 0 0 12px; font-size: 20px; letter-spacing: -0.01em;">Available now</h2>

          <div class="cards">
            {tools.slice(0, 1).map((t) => (
              <a
                class={`card ${t.disabled ? "disabled" : ""}`}
                href={t.disabled ? "#" : t.href}
                aria-disabled={t.disabled ? "true" : "false"}
                tabindex={t.disabled ? "-1" : "0"}
                style="grid-column: 1 / -1;"
              >
                <div class="cardTop">
                  <span class="badge">{t.tag}</span>
                  <span class={`badge ${t.status === "Live" ? "badgeLive" : "badgeSoon"}`}>
                    {t.status}
                  </span>
                </div>
                <h3>{t.name}</h3>
                <p>{t.desc}</p>
              </a>
            ))}
          </div>

          <div class="cta" style="margin-top: 14px;">
            <a class="btn btnPrimary" href="/tools">See all tools</a>
          </div>
        </section>

        <section style="margin-top: 36px; padding-top: 24px; border-top: 1px solid var(--stroke);">
          <h2 style="margin: 0 0 12px; font-size: 16px; letter-spacing: -0.01em; color: var(--muted);">About this site</h2>
          <p style="margin: 0; color: var(--muted); max-width: 70ch; line-height: 1.6; font-size: 14px;">
            I built these tools for myself. They solve real problems I have: quick reference while buying, fast math for spot prices, inventory tracking. If they're useful to you too, great.
          </p>
        </section>

        <footer style="margin-top: 32px; padding-top: 24px; border-top: 1px solid var(--stroke); display: flex; gap: 16px; align-items: center; flex-wrap: wrap;">
          <div style="display: flex; gap: 12px; flex-wrap: wrap;">
            {links.map((l) => (
              <a class="btn" href={l.href} target="_blank" rel="noreferrer">{l.name}</a>
            ))}
          </div>
          <span style="color: var(--faint); font-size: 12px;">Built with Astro</span>
        </footer>
      </main>
    </div>
  </body>
</html>
