---
const tools = [
  {
    name: "Coin & Currency Scout",
    desc: "Spot prices, melt calculator, key dates, and inventory. Built for quick decisions at home or at auctions.",
    href: "/tools/coin-scout",
    status: "Live",
    tag: "Tools",
  },
  {
    name: "Coin Library (coming soon)",
    desc: "Quick reference for types, compositions, key dates, and red flags.",
    href: "/tools",
    status: "Soon",
    tag: "Reference",
    disabled: true,
  },
  {
    name: "Auction Mode (coming soon)",
    desc: "Fast entry, totals, targets, and sanity checks while bidding.",
    href: "/tools",
    status: "Soon",
    tag: "Workflow",
    disabled: true,
  },
];

const links = [
  { name: "GitHub", href: "https://github.com/JacobDrivers" },
  { name: "LinkedIn", href: "https://www.linkedin.com/in/jacobadrury/" },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Jacob Drury | Tools + Projects</title>
    <meta
      name="description"
      content="Personal site for small, useful tools and projects. Fast, shareable, and practical."
    />
    <style>
      :root {
        --bg: #0b1220;
        --panel: rgba(255, 255, 255, 0.06);
        --panel2: rgba(255, 255, 255, 0.09);
        --text: rgba(255, 255, 255, 0.92);
        --muted: rgba(255, 255, 255, 0.72);
        --faint: rgba(255, 255, 255, 0.55);
        --stroke: rgba(255, 255, 255, 0.10);
        --accent: #f59e0b;
        --good: #34d399;
        --shadow: 0 20px 60px rgba(0, 0, 0, 0.45);
        --radius: 16px;
      }

      * { box-sizing: border-box; }
      html, body { height: 100%; }
      body {
        margin: 0;
        background:
          radial-gradient(1200px 800px at 20% -10%, rgba(245, 158, 11, 0.18), transparent 60%),
          radial-gradient(900px 700px at 110% 20%, rgba(52, 211, 153, 0.10), transparent 55%),
          radial-gradient(1000px 800px at 50% 120%, rgba(59, 130, 246, 0.10), transparent 60%),
          var(--bg);
        color: var(--text);
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      }

      a { color: inherit; text-decoration: none; }
      a:focus-visible { outline: 2px solid rgba(245, 158, 11, 0.7); outline-offset: 3px; border-radius: 10px; }

      .wrap {
        max-width: 1050px;
        margin: 0 auto;
        padding: 28px 20px 60px;
      }

      header {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 18px;
        margin-top: 14px;
      }

      .brand h1 {
        font-size: clamp(28px, 3.4vw, 40px);
        letter-spacing: -0.02em;
        margin: 0 0 8px;
        line-height: 1.05;
      }

      .brand p {
        margin: 0;
        color: var(--muted);
        max-width: 60ch;
        line-height: 1.5;
      }

      .pillbar {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        align-items: center;
        justify-content: flex-end;
      }

      .pill {
        border: 1px solid var(--stroke);
        background: var(--panel);
        padding: 8px 12px;
        border-radius: 999px;
        color: var(--muted);
        font-size: 13px;
        display: inline-flex;
        gap: 8px;
        align-items: center;
      }

      .dot {
        width: 8px;
        height: 8px;
        border-radius: 999px;
        background: var(--good);
        box-shadow: 0 0 0 3px rgba(52, 211, 153, 0.12);
      }

      .hero {
        margin-top: 26px;
        border: 1px solid var(--stroke);
        background: linear-gradient(180deg, var(--panel2), rgba(255,255,255,0.03));
        border-radius: var(--radius);
        padding: 18px;
        box-shadow: var(--shadow);
      }

      .heroRow {
        display: grid;
        grid-template-columns: 1.25fr 0.75fr;
        gap: 14px;
      }

      @media (max-width: 860px) {
        .heroRow { grid-template-columns: 1fr; }
        header { flex-direction: column; align-items: flex-start; }
        .pillbar { justify-content: flex-start; }
      }

      .cta {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        margin-top: 14px;
      }

      .btn {
        border: 1px solid var(--stroke);
        background: rgba(255,255,255,0.06);
        padding: 10px 14px;
        border-radius: 12px;
        font-weight: 600;
        font-size: 14px;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        transition: transform 120ms ease, background 120ms ease, border-color 120ms ease;
      }
      .btn:hover { transform: translateY(-1px); background: rgba(255,255,255,0.09); border-color: rgba(255,255,255,0.18); }
      .btnPrimary {
        background: rgba(245, 158, 11, 0.14);
        border-color: rgba(245, 158, 11, 0.35);
      }
      .btnPrimary:hover {
        background: rgba(245, 158, 11, 0.20);
        border-color: rgba(245, 158, 11, 0.48);
      }

      .cards {
        margin-top: 18px;
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 12px;
      }

      @media (max-width: 980px) {
        .cards { grid-template-columns: repeat(2, 1fr); }
      }
      @media (max-width: 620px) {
        .cards { grid-template-columns: 1fr; }
      }

      .card {
        border: 1px solid var(--stroke);
        background: rgba(255,255,255,0.05);
        border-radius: var(--radius);
        padding: 14px;
        min-height: 140px;
        transition: transform 120ms ease, background 120ms ease, border-color 120ms ease;
      }
      .card:hover { transform: translateY(-1px); background: rgba(255,255,255,0.07); border-color: rgba(255,255,255,0.18); }
      .card.disabled { opacity: 0.60; }
      .card.disabled:hover { transform: none; }

      .cardTop {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
      }

      .badge {
        font-size: 12px;
        color: rgba(255,255,255,0.78);
        border: 1px solid var(--stroke);
        background: rgba(255,255,255,0.05);
        padding: 6px 10px;
        border-radius: 999px;
        white-space: nowrap;
      }

      .badgeLive { border-color: rgba(52, 211, 153, 0.30); background: rgba(52, 211, 153, 0.10); }
      .badgeSoon { border-color: rgba(255, 255, 255, 0.16); }

      .card h3 { margin: 10px 0 6px; font-size: 16px; letter-spacing: -0.01em; }
      .card p { margin: 0; color: var(--muted); line-height: 1.45; font-size: 14px; }

      .footer {
        margin-top: 18px;
        color: var(--faint);
        font-size: 13px;
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        align-items: center;
      }

      .sep { opacity: 0.35; }
    </style>
  </head>

  <body>
    <div class="wrap">
      <header>
        <div class="brand">
          <h1>Jacob Drury</h1>
          <p>
            A small, practical site for tools and experiments I actually use.
            If you need a quick answer or calculator, it should be here.
          </p>
        </div>

        <div class="pillbar">
          <span class="pill"><span class="dot"></span> Live deploy via Cloudflare Pages</span>
          <span class="pill">Astro + TypeScript</span>
          <span class="pill">Fast + static-first</span>
        </div>
      </header>

      <section class="hero">
        <div class="heroRow">
          <div>
            <div class="pill" style="display:inline-flex; margin-bottom: 10px;">
              Tools hub
              <span class="sep">•</span>
              shareable links
              <span class="sep">•</span>
              no noise
            </div>

            <div style="font-size: 18px; line-height: 1.5; color: var(--muted);">
              Start with the tools below. More will show up as I build them.
            </div>

            <div class="cta">
              <a class="btn btnPrimary" href="/tools/coin-scout">Open Coin & Currency Scout</a>
              <a class="btn" href="/tools">Browse all tools</a>
            </div>
          </div>

          <div>
            <div class="card" style="min-height: auto;">
              <div class="cardTop">
                <span class="badge">Now</span>
                <span class="badge badgeLive">Live</span>
              </div>
              <h3 style="margin-top: 12px;">Coin & Currency Scout</h3>
              <p>
                Melt math, quick ID notes, and an inventory flow aimed at real use.
              </p>
            </div>

            <div class="footer" style="margin-top: 12px;">
              {links.map((l) => (
                <>
                  <a class="btn" href={l.href} target="_blank" rel="noreferrer">{l.name}</a>
                </>
              ))}
            </div>
          </div>
        </div>

        <div class="cards">
          {tools.map((t) => (
            <a
              class={`card ${t.disabled ? "disabled" : ""}`}
              href={t.disabled ? "#" : t.href}
              aria-disabled={t.disabled ? "true" : "false"}
              tabindex={t.disabled ? "-1" : "0"}
            >
              <div class="cardTop">
                <span class="badge">{t.tag}</span>
                <span class={`badge ${t.status === "Live" ? "badgeLive" : "badgeSoon"}`}>
                  {t.status}
                </span>
              </div>
              <h3>{t.name}</h3>
              <p>{t.desc}</p>
            </a>
          ))}
        </div>

        <div class="footer">
          <span>Built for quick sharing and real utility.</span>
          <span class="sep">•</span>
          <span>More tools will be added under <code>/tools</code>.</span>
        </div>
      </section>
    </div>
  </body>
</html>
